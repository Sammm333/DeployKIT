services:
  deploykit:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./repos:/app/repos
    environment:
      - DOCKER_PATH=/usr/bin/docker